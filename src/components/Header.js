// import React from 'react'; --> was required but not anymore
// still needed if using class component

const Header = () => {
    return (
        <header>
            <h1>This is the header component</h1>
        </header>
    )
};

export default Header
// self contained MODULE unit that can move assets between files